// uniforms.js — auto-detected uniform sidebar controls
// Wired up in a later milestone.

/**
 * Render UI controls for the given uniform descriptors into container.
 * @param {Array<{type: string, name: string}>} uniforms
 * @param {HTMLElement} container
 */
export function buildControls(uniforms, container) {
  // TODO: generate sliders, color pickers, toggles etc.
  void uniforms; void container;
}

/**
 * Return a map of { uniformName: currentValue } for all active controls.
 */
export function getUniformValues() {
  return {};
}
